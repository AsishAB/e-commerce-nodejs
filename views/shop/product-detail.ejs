<%- include('../layouts/main-layout.ejs') %>


<main>
    <div class="px-4 pt-5 my-5 text-center border-bottom">
        <h1 class="display-4 fw-bold"><%= product.TP_Product_Title %></h1>
        <div class="col-lg-6 mx-auto">
          <p class="lead mb-4"><%= product.TP_Product_Description %></p>
          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mb-5">
            <form method="POST" action="/shop/addToCart">
              <button class="btn btn-primary btn-lg px-4 me-sm-3">Add to Cart</a>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <input type="hidden" name="productId" id="productId" value="<%= product._id %>">
            </form>
            
          </div>
        </div>
        <div class="overflow-hidden" style="max-height: 30vh;">
          <div class="container px-5">
            <img src="<%= product.TP_Image_URL %>" class="img-fluid border rounded-3 shadow-lg mb-4" alt="<%= product.title %>" width="200" height="200" loading="lazy">
          </div>
        </div>
      </div>
    
</main>




<%- include('../layouts/footer.ejs') %>